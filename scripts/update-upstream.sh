#!/usr/bin/env bash
set -euo pipefail
printf '%s\n' 'Las actualizaciones son deliberadas: edita upstream.lock en una rama, elimina el workspace reconstruible y ejecuta bootstrap.sh + verify.sh antes de confirmar el cambio.'
