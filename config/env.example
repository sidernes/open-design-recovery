# Copia solo las variables que realmente necesites a un archivo privado.
# Nunca confirmes claves API, tokens o credenciales en Git.

# OD_DATA_DIR=/ruta/privada/open-design-data
# OD_MEDIA_CONFIG_DIR=/ruta/privada/media-config
